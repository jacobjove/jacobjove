---
to: apps/web/generated/models/<%= Name %>/hooks/postUpdate.ts
unless_exists: true
---
/* Edit this file to add a non-default post-save hook for the <%= Name %> type. */

import <%= Name %>Document from "@web/generated/interfaces/<%= Name %>";

export const postUpdate = async (<%= name %>: <%= Name %>Document, updatedFields: any) => {
  return <%= name %>;
}

---
to: apps/web/components/data/<%= h.inflection.pluralize(name) %>/generated/<%= Name %>CreationDialog.tsx
force: true
---

import CreationDialog from "@web/components/data/CreationDialog";
import { useCreate<%= Name %>, use<%= Name %>Reducer } from "@web/generated/hooks/<%= name %>.hooks";
import { bindPopover } from "material-ui-popup-state/hooks";
import { <%= Name %>CreationInput } from "@web/graphql/generated/inputs/<%= name %>.inputs";
import <%= Name %> from "@web/graphql/generated/types/<%= Name %>";
import { <%= Name %>Fragment } from "@web/graphql/generated/fragments/<%= name %>.fragment";
import fields from "@web/graphql/generated/fields/<%= name %>.fields";

export type <%= Name %>CreationDialogProps = ReturnType<typeof bindPopover>;

export default function <%= Name %>CreationDialog(props: <%= Name %>CreationDialogProps) {
  const [create,] = useCreate<%= Name %>();
  const dataTuple = use<%= Name %>Reducer();
  return CreationDialog<<%= Name %>, <%= Name %>CreationInput, { create<%= Name %>: <%= Name %>Fragment }>({
    typeName: "<%= name %>",
    dataTuple,
    create,
    fields,
    // produceInitialData,
    ...props,
  });
}

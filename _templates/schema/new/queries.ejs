---
to: graphql/schema/generated/queries/<%= name %>.queries.ts
force: true
---
/* Do not edit this file. It was generated programmatically. */

import { gql } from "@apollo/client";
import { <%= name %>Fragment } from "@/graphql/schema/generated/fragments/<%= name %>.fragment";

export const GET_<%= h.changeCase.constantCase(name) %> = gql`
  query Get<%= Name %>($where: <%= Name %>WhereUniqueInput!) {
    <%= name %>(where: $where) {
      ...<%= Name %>Fragment
    }
  }
  ${<%= name %>Fragment}
`;

export const GET_<%= h.changeCase.constantCase(h.inflection.pluralize(name)) %> = gql`
  query Get<%= h.inflection.pluralize(Name) %> {
    <%= name %>s {
      ...<%= Name %>Fragment
    }
  }
  ${<%= name %>Fragment}
`;

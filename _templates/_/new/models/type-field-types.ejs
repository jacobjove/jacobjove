---
to: apps/web/generated/models/<%= Name %>/types.ts
force: true
---
/* Do not edit this file. It was generated programmatically. */
<% var entries = Object.entries(fields).filter(([,field]) => (field.type === "Map" && field.shape)); -%>
<% if (entries.length) { -%>
<% for (var [fieldName, field] of Object.entries(fields).filter(([,field]) => (field.type === "Map" && field.shape))) { -%>
export class <%= h.changeCase.pascal(fieldName) %> {
<% for (var [key, value] of Object.entries(field.shape)) { -%>
  <%= key %><%- (value.required || typeof value.default !== "undefined") ? "!" : "?" %>: <%- getType(value) %><%- (typeof value.nullable !== "undefined" ? !value.nullable : value.required) ? "" : " | null" %>;
<% } -%>
}

<% } -%>
<% } else { -%>
export {};
<% } -%>

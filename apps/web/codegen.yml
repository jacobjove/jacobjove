overwrite: true
schema: "http://localhost:3000/api/graphql"
generates:
  # src/generated/graphql.ts:
  #   plugins:
  #     - "fragment-matcher"
  graphql/schema.gql.json:
    plugins:
      - "introspection"
    config:
      minify: true

/* Do not edit this file. It was generated programmatically. */

import { Note } from "@web/generated/interfaces/Note";
import { NoteDocument } from "@web/generated/models/Note/document";
import { postCreate, postUpdate, preSave } from "@web/generated/models/Note/hooks";
import { DEFAULT_SCHEMA_OPTIONS } from "@web/graphql/schema/types";
import mongoose, { ModifyResult, UpdateQuery } from "mongoose";
import mongooseLeanVirtuals from "mongoose-lean-virtuals";

const noteSchema = new mongoose.Schema<Note>(
  {
    userId: {
      type: mongoose.Schema.Types.ObjectId,
      ref: "User",
      required: true,
    },
    notebookId: {
      type: mongoose.Schema.Types.ObjectId,
      ref: "Notebook",
      required: true,
    },
    title: {
      type: String,
      trim: true,
      required: true,
    },
    body: {
      type: String,
      trim: true,
      required: false,
      default: "",
    },
    public: {
      type: Boolean,
      required: false,
      default: null,
    },
    archivedAt: { type: Date, required: false, default: null },
  },
  DEFAULT_SCHEMA_OPTIONS
);

noteSchema.plugin(mongooseLeanVirtuals);

noteSchema.pre<NoteDocument>("save", async function () {
  console.log("Saving Note", this);
  await preSave(this);
});

noteSchema.post<NoteDocument>("save", async function (document) {
  console.log("Saved Note", document);
  await postCreate(document);
});

noteSchema.post<NoteDocument>(
  "findOneAndUpdate",
  async function (_result: NoteDocument | ModifyResult<NoteDocument>) {
    // eslint-disable-next-line @typescript-eslint/no-this-alias
    const query = this as unknown as UpdateQuery<NoteDocument>;
    const updatedFields = query.getUpdate().$set;
    if ((_result as ModifyResult<NoteDocument>).value) {
      const result = _result as ModifyResult<NoteDocument>;
      const note = result.value;
      if (note) {
        if (!result.lastErrorObject?.updatedExisting) {
          await postCreate(note);
        } else {
          await postUpdate(note, updatedFields);
        }
      }
    } else {
      const result = _result as NoteDocument;
      await postUpdate(result, updatedFields);
    }
  }
);

export { noteSchema };

// https://stackoverflow.com/questions/19051041/cannot-overwrite-model-once-compiled-mongoose
export const NoteModel = mongoose.models.Note || mongoose.model<Note>("Note", noteSchema);

export default NoteModel;

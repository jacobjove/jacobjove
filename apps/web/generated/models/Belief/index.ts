/* Do not edit this file. It was generated programmatically. */

import { Belief } from "@web/generated/interfaces/Belief";
import { BeliefDocument } from "@web/generated/models/Belief/document";
import { postCreate, postUpdate, preSave } from "@web/generated/models/Belief/hooks";
import { DEFAULT_SCHEMA_OPTIONS } from "@web/graphql/schema/types";
import mongoose, { ModifyResult, UpdateQuery } from "mongoose";
import mongooseLeanVirtuals from "mongoose-lean-virtuals";

const beliefSchema = new mongoose.Schema<Belief>(
  {
    userId: {
      type: mongoose.Schema.Types.ObjectId,
      ref: "User",
      required: true,
    },
    name: {
      type: String,
      trim: true,
      required: true,
    },
    description: {
      type: String,
      trim: true,
      required: false,
      default: null,
    },
    archivedAt: { type: Date, required: false, default: null },
  },
  DEFAULT_SCHEMA_OPTIONS
);

beliefSchema.plugin(mongooseLeanVirtuals);

beliefSchema.pre<BeliefDocument>("save", async function () {
  console.log("Saving Belief", this);
  await preSave(this);
});

beliefSchema.post<BeliefDocument>("save", async function (document) {
  console.log("Saved Belief", document);
  await postCreate(document);
});

beliefSchema.post<BeliefDocument>(
  "findOneAndUpdate",
  async function (_result: BeliefDocument | ModifyResult<BeliefDocument>) {
    // eslint-disable-next-line @typescript-eslint/no-this-alias
    const query = this as unknown as UpdateQuery<BeliefDocument>;
    const updatedFields = query.getUpdate().$set;
    if ((_result as ModifyResult<BeliefDocument>).value) {
      const result = _result as ModifyResult<BeliefDocument>;
      const belief = result.value;
      if (belief) {
        if (!result.lastErrorObject?.updatedExisting) {
          await postCreate(belief);
        } else {
          await postUpdate(belief, updatedFields);
        }
      }
    } else {
      const result = _result as BeliefDocument;
      await postUpdate(result, updatedFields);
    }
  }
);

export { beliefSchema };

// https://stackoverflow.com/questions/19051041/cannot-overwrite-model-once-compiled-mongoose
export const BeliefModel = mongoose.models.Belief || mongoose.model<Belief>("Belief", beliefSchema);

export default BeliefModel;

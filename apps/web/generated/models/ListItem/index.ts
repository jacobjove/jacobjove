/* Do not edit this file. It was generated programmatically. */

import { ListItem } from "@web/generated/interfaces/ListItem";
import { postCreate, postUpdate, preSave } from "@web/generated/models/ListItem/hooks";
import { DEFAULT_SCHEMA_OPTIONS } from "@web/graphql/schema/types";
import mongoose, { HydratedDocument, ModifyResult, UpdateQuery } from "mongoose";
import mongooseLeanVirtuals from "mongoose-lean-virtuals";

const listItemSchema = new mongoose.Schema<ListItem>(
  {
    userId: {
      type: mongoose.Schema.Types.ObjectId,
      ref: "User",
      required: true,
    },
    listId: {
      type: mongoose.Schema.Types.ObjectId,
      ref: "List",
      required: true,
    },
    data: {
      type: Object,
      required: true,
    },
    archivedAt: { type: Date, required: false, default: null },
  },
  DEFAULT_SCHEMA_OPTIONS
);

listItemSchema.plugin(mongooseLeanVirtuals);

listItemSchema.pre<HydratedDocument<ListItem>>("save", async function () {
  return Promise.resolve(preSave(this));
});

listItemSchema.post<ListItem>("save", async function (document) {
  await postCreate(document);
});

listItemSchema.post<ListItem>(
  "findOneAndUpdate",
  async function (_result: ListItem | ModifyResult<ListItem>) {
    // eslint-disable-next-line @typescript-eslint/no-this-alias
    const query = this as unknown as UpdateQuery<ListItem>;
    const updatedFields = query.getUpdate().$set;
    if ((_result as ModifyResult<ListItem>).value) {
      const result = _result as ModifyResult<ListItem>;
      const listItem = result.value;
      if (listItem) {
        if (!result.lastErrorObject?.updatedExisting) {
          await postCreate(listItem);
        } else {
          await postUpdate(listItem, updatedFields);
        }
      }
    } else {
      const result = _result as ListItem;
      await postUpdate(result, updatedFields);
    }
  }
);

export { listItemSchema };

// https://stackoverflow.com/questions/19051041/cannot-overwrite-model-once-compiled-mongoose
export const ListItemModel: mongoose.Model<ListItem> =
  mongoose.models.ListItem || mongoose.model<ListItem>("ListItem", listItemSchema);

export default ListItemModel;
